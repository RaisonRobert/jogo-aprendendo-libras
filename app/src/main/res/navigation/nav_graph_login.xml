<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_login"
    app:startDestination="@id/login_inicial">
    <fragment
        android:id="@+id/login_inicial"
        android:name="com.edu.pucpr.raison.jogoaprendendolibras.view.fragment.LoginFragment"
        android:label="Dialog Cadastro"
        tools:layout="@layout/layout_fragment_login">
        <action
            android:id="@+id/action_dialog_cadastro_to_menuInicial"
            app:destination="@id/login_inicial"
            />
    </fragment>
</navigation>